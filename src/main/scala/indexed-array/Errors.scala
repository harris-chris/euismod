package sportarray

object ErrorsObj {
  trait IsError

  import ArrayDefs._

  //case class IndexOutOfBound[A[_], T] (
    //sh: ShapeRT[A[T], HNil],
    //arr: A[T],
    //idx: Int,
  //) extends IsError {
    //def show: String = 
      //s"Array ${arr} with shape ${sh(arr)} has no index ${idx}"
  //}
}
